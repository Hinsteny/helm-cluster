apiVersion: v1
kind: Service
metadata:
  name: {{ include "seata-samples.fullname" . }}
  labels:
    {{- include "seata-samples.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seata-samples.selectorLabels" . | nindent 4 }}
