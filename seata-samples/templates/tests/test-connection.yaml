apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seata-samples.fullname" . }}-test-connection"
  labels:
    {{- include "seata-samples.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seata-samples.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
